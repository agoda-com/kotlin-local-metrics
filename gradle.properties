org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.parallel=true
# Stdlib is pinned explicitly per-module (1.9.25) so published POMs stay
# consumable by Kotlin 1.9 compilers; disable the default (2.x) stdlib.
kotlin.stdlib.default.dependency=false
